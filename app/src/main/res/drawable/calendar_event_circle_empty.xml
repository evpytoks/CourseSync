<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="@android:color/transparent" />
    <stroke
        android:width="2dp"
        android:color="@color/cs_outline" />
    <size
        android:width="@dimen/calendar_event_done_icon_size"
        android:height="@dimen/calendar_event_done_icon_size" />
</shape>
